const initialCustomerState = {
  fullName: "",
  nationalId: "",
};

export const customerReducer = (state = initialCustomerState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
